1. Check current branch and git status
2. If on main branch, create and checkout a new feature branch
3. Run git add all files
4. Write a commit message and commit changes
5. Push changes to remote repository
6. Create a pull request to main branch using GitHub CLI
